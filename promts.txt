1 Напиши скрипт на Python, який зчитує дані з Google Таблиці за допомогою бібліотеки Pandas (через експорт посилання у форматі /export?format=xlsx).

Врахуй особливості структури (згідно з файлом image_00fcfd.png):

    Кілька аркушів: У таблиці багато листів із різними назвами. Код повинен автоматично обробляти всі наявні аркуші (sheet_name=None).

    Складна "шапка": Перші 3–4 рядки — це технічна інформація (назва групи, дата оновлення, посилання на роботи). Їх потрібно відсікти, щоб дані починалися з ПІБ студентів.

    Ідентифікація: Перший стовпець містить імена студентів (наприклад, "Білошапка Олександр"). Цей стовпець має стати індексом (index).

    Оцінки: Решта стовпців містять числові оцінки. Потрібно передбачити, що в таблиці є порожні клітинки (де оцінки ще не виставлені) — їх треба обробити (наприклад, замінити на NaN або 0).

Вимоги до коду:

    Використовуй pd.read_excel(url, sheet_name=None, header=None).

    Напиши цикл, який проходить по кожному аркушу, видаляє верхні зайві рядки та порожні стовпці/рядки в кінці.

    На виході має вийти словник (Dictionary), де ключем є назва аркуша, а значенням — чиста таблиця (DataFrame) з оцінками.

    Додай приклад того, як з отриманого результату витягнути список оцінок для конкретного студента за його прізвищем.

  2  Зроби щоб обходились усі аркуші і виводились в консоль оцінки студента що я оберу
3 Зроби щоб повнісню вся таблиця імпортувалася в JSON
4 Додай функцію яка буде вираховувати середню арифметичну для одного студенту на вибір все реалізуй в individual_marks.py
5 Дивись мені треба написати функцію, для розкалду, яка на початку ранку о 8:30 буде скидувати розклад занять на день, і потім попередження перед кожною парою,
є те що існує система "чисельник/знаменник", як це реалізувати краще?

6 Напиши окремий модуль (файл scheduler_logic.py) для існуючого Telegram-бота, який автоматизує розсилку розкладу та нагадувань.

Context:
Користувач — студент, що проживає у місті Дніпро. Розклад базується на системі "Чисельник/Знаменник".

Schedule Data (з файлу image_4ca0a0.png):
Структуруй дані у вигляді словника (Dictionary), де ключі — індекси днів тижня (0-4), а значення — списки занять з полями time, subject та type (both, num, den).

    Понеділок: 10:30 (num: Створення та візуалізація / den: ООП), 12:30 (Організація КМ), 14:00 (Іноземна мова), 15:30 (den: ООП II).

    Вівторок: 09:00 (Філософія), 10:30 (Фізкультура), 12:30 (Алгоритми), 14:00 (Класна година).

    Середа: 09:00 (Створення та візуалізація I), 10:30 (ООП), 12:30 (Іноземна мова), 14:00 (Створення та візуалізація II).

    Четвер: 09:00 (ООП I), 10:30 (Алгоритми), 12:30 (num: Філософія / den: Організація КМ), 14:00 (ООП II), 15:30 (Алгоритми II).

    П'ятниця: 09:00 (Організація КМ I), 10:30 (Алгоритми), 12:30 (Іноземна мова), 14:00 (Організація КМ II).

Functional Requirements:

    Week Logic: Використовуй ISO-номер тижня. Непарний = Чисельник (num), Парний = Знаменник (den).

    Daily Summary: Функція, що спрацьовує о 08:30 і надсилає список пар на сьогодні.

    Smart Reminders: Функція нагадування, що спрацьовує рівно за 10 хвилин до початку кожної пари (наприклад, для пари о 09:00 нагадування має бути о 08:50).

    Setup Function: Головна функція setup_scheduler(bot, chat_id), яка ініціалізує AsyncIOScheduler з часовим поясом Europe/Kyiv та додає всі завдання.

Сам розклад засунь в файл БД наприкдад JSON

7 Додай команди на те щоб отримати в чатботі всі оцінки, оцінки учня на вибір, та розклад
8 Зроби щоб імя студента вводилось користувачем динамічно під час роботи програми після того як він натиснув кнопку